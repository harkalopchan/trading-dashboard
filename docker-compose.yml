services:
  backend:
    build:
      context: ./backend
    container_name: trading-dashboard-backend
    ports:
      - "3000:3000"

  frontend:
    build:
      context: ./frontend
    container_name: trading-dashboard-frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend